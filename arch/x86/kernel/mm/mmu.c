// MMU(Memory Management Unit) — a hardware block in the CPU that translates
// virtual addresses into physical ones. It works with page tables, TLB(Translation Lookaside Buffer),
// and enforces memory protection(access rights, read / write permissions, etc.).
